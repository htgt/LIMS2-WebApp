#!/usr/bin/env perl

use strict;
use warnings FATAL => 'all';

use LIMS2::Model;

my $model = LIMS2::Model->new( user => 'tasks' );

my $transcript = $model->ensembl_transcript_adaptor('mouse')->fetch_by_stable_id('ENSMUST00000191642');

die "Couldn't get it" unless $transcript;

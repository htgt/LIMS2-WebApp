INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'A01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = '100';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'A02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = '100';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'A03', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = '100';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'A01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'PCS00177_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'A02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'PCS00177_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'A03', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'PCS00177_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'M01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'PCS00177_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'I01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'I02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'I03', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'I04', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'J01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'J02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'J03', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'K01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'K02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'M01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAS0001_A';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAQ0001_A_2';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N02', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'MOHSAQ0001_A_2';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'FEP0006';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'SEP0006';

INSERT INTO wells(name, plate_id, created_by_id)
SELECT 'N01', plates.id, plates.created_by_id
FROM plates
WHERE plates.name = 'FEPD0006_1';
